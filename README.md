# Dilute Online v2

A polished cap table and dilution planning platform for founders.

## Run Locally

```bash
npm install
npm run dev
```

## Deploy To Vercel

The project includes `vercel.json` with Vite build settings and SPA route rewrites.

- Install command: `npm install`
- Build command: `npm run build`
- Output directory: `dist`
